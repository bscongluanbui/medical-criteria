# Optional local development build; VPS uses the default pull-only compose.
services:
  drive-sync:
    build:
      context: .
      target: runtime
  migrate:
    build:
      context: .
      target: runtime
  api:
    build:
      context: .
      target: runtime
  dashboard:
    build:
      context: .
      target: runtime
